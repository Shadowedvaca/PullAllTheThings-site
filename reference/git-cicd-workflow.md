# Retired Git and CI/CD Workflow

This former personal-standard copy is retired because it duplicated repository
behavior and authorized a hotfix path that could bypass test validation.

Use these authoritative sources:

- `reference/work-management.md` for Solo Development, hierarchy, cadence,
  branches/PRs, approvals, evidence, and closure;
- `reference/development-and-release.md` for quality gates, environments,
  promotion, versioning, release notes, migrations, health, rollback, and
  release evidence; and
- `.github/workflows/` for the implemented automation.

No hotfix, branch name, tag, or operational urgency bypasses required automated
checks, Promotion to test approval, successful test evidence, or Promotion to
production approval for Mike's exact selected tag.
